> the whole of idea of making a project was that it could benefit more than any other thing in the 
world and also that I could make it myself, that also touch the part where I apply my programming 
skills
>  so I was having not problems as such rather not missing a platform to store typically only links, 
a platform which I can access anytime and store any url to access later, cause see we guys in tech 
read a lot, visit through a lot of stuff, be it a news article, a X post having some UI stacks which would  
help further when making a project,a motivational YouTube video you wanna see when later etc etc, the 
use cases I found very much and initially it was for me only, I started coding and made a pretty much 
a decent "kaam chalau" application but I thought why not polish it a bit and make it production worthy
like other apps out there, so that was it, I decided to make it productionize it 

~(Stack learnt along the line)
> earlier when I was just designing the UI and wrote a very decent backend, and when I used to paste 
 Git url's I saw a very pretty image,also called "wildcards", so I researched about what are those? how are 
these implemented? can I use it myself or it's just something that popular apps can only do? and the answer 
Was OpenGraph tags, you see for some sites when you paste a url maybe in a whatsapp chat or anywhere else
you see that url comes with an image rendered, that's being displayed there because of og:tags (opengraph) 
tags, it acts like a crawler and fetches looks for those tags like og:title, og:description, og:image etc and it 
simply displays it, I was amazed by this and wanted to try this out, I pasted many url's and almost every
professional app has these og:tags written inside their codebase, I also did the same thing, updated my 
layout.tsx added these tags and boom💥, my application also displays pretty image, has a nice title & desc-
-ription, other details as well

~(Writing the backend for scraping og:tags)
> but now the question was how can I write a backend which scrapes these tags i.e. acts like a crawler
eg: Google crawler, the answer was to first of all accept a valid url (add decent backend validation) and then make 
a fetch request to that link from inside my POST request handler, sounds weird right? making a fetch request 
from a POST handler, yes!!!! my initial reaction was similar, like can we do this? are we supposed to do this?
however the solution was this is exactly how we do this. When we have a valid url we wanna disguise our server
and make it act like a browser and not a bot which websites will not send the data with appropriate headers and with a capital APPROPRIATE headers, 
without the exact headers, we cannot scrape a site, cause the site we are looking for also has added security 
which blocks certain bots, we don't want our "server" acting like a bot, we want it to be a scraper / crawler 
by which we can get "og:tags" 

(Headers)
> now let's dive into the technical stuff how i wrote my backend what are these so called headers I am talking about, 
so when make simple request to fetch data from server, our server expects some specific headers, just like that we also have to 
add specific headers, so User-Agent is the right header we have to add, to let the server know that this website which is requesting 
the og:tags is an actual browser, but is it over yet? no! we also have to add more things like : 
User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64), now the destcturing of header string is as follows :


HTTP HEADERS EXPLANATION
User-Agent Header
"User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36"
Mozilla/5.0: Tells the server the request is coming from a browser (not a bot or script)
Windows NT 10.0; Win64; x64: Client device specifications
  - Windows NT → Windows operating system
  - Win64 → 64-bit device architecture
  - x64 → 64-bit CPU architecture
Note: These exact specs don't matter - a Mac user can send these same headers. It's just for mimicking a real browser to bypass bot detection.
AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36: Browser rendering engine details (makes it look like Chrome browser)
---
Accept Header
"Accept": "text/html,application/xhtml+xml,application/xml;q=0.9,image/webp,*/*;q=0.8"
Tells the server which content types we can accept:
  - text/html → Standard HTML pages
  - application/xhtml+xml → XHTML format
  - application/xml;q=0.9 → XML with quality factor 0.9 (lower priority)
  - image/webp → WebP image format
  - /;q=0.8 → Any other format (lowest priority)
The q= values indicate preference (higher = more preferred)
---
Accept-Language Header
"Accept-Language": "en-US,en;q=0.9"
Specifies which languages the client can understand:
  - en-US → US English (highest priority, q=1.0 by default)
  - en;q=0.9 → Generic English (slightly lower priority)
---
Accept-Encoding Header
"Accept-Encoding": "gzip, deflate, br"
Tells the server we can accept compressed responses:
  - gzip → GZip compression
  - deflate → Deflate compression
  - br → Brotli compression (modern, better compression)
Some websites send compressed data to reduce bandwidth - this header says "we can decompress it"
---
Connection Header
"Connection": "keep-alive"
Keep the TCP connection open after the current request completes, allowing multiple requests to use the same connection (more efficient)
---
Upgrade-Insecure-Requests Header
"Upgrade-Insecure-Requests": "1"
Tells the server: "If you have an HTTPS version of this resource, send me that instead of HTTP"
This is a security feature that:
  - Signals the client prefers secure (HTTPS) connections
  - Helps servers automatically upgrade HTTP requests to HTTPS
  - Prevents downgrade attacks
  - Makes the client look like a modern, security-aware browser
In your code: Since you already normalize URLs to https:// (line 36), this is somewhat redundant but still good practice for bot detection avoidance.
---
Cache Option
cache: "no-store"
Don't cache the response - always fetch fresh data from the server. This ensures you always get the latest version of the page content (important for link previews that might change).

after this we add extract the response sent by the website's server and load it into a text format, this response is in a text html 
document which we cannot search, so the tags sent by the server we cannot access them, for that we use js library cheerio.js 
actually search from the text document, the tags in the <meta> tag so write the a simple extraction function to extract those 
tags and sent the rich images, proper title, description and other tags to the frontend and eventually to the user 

(How does the user saves link?)


we're diving into an interesting aspect cause the technicality is not over, we're into frontend territory now, how actually does the user
saves link and, the flow of the application, so initially there was no DB, the links were ephemeral, my backend didn't have DB, no Auth, no 
schema, no proper maintenance, as I was not having really the time to sit for couple of hours and add a backend, finally I skipped my 
lectures for whole day and decied to complete auth, so what's the best backend stack which works excellent Nextjs? if you guessed Supabase, 
you're my friend without introduction, and if you guessed that I was working with Nextjs! boom! we're best buds, consider pinging me we can 
build something together can attend a hackathon together, so back to topic , I decided my backend schema, wrote it down thought actually 
the schema was decided earlier only, all that was remaining is to initialize a new Supabase project and add the schema, however auth was 
needed first, (yes, I know, I am not that dumb even if I sound like that!!!) so I added social logins (Google & GitHub) and btw who uses email 
logins in 2025?? that's just cliche, it took me a lotta time to simply add authentication, IDK why it took time maybe I was drained while doing it
or not in a correct mental state but it took time, after couple of hours I completed adding auth, and then eventually DB schema got added, I updated
my backend once again to save the query first to DB and then send it to frontend, also added a GET route which fetched the specific user's links

backend part completely over now, how user get's the links


User arrives at website, has to authenticates ofcourse to submit links 

paste the url in the input box, give some time to backend to fetch the tags

Boom! there you have you're link saved, appers in a beautiful image right?

users can simply type :  github.com or www.github.com or https://github.com or even http://github.com  
we (means I) got you!!! my backend handles all the edge case

~ Edge cases 


What if some links don't show preview? 


some websites do not share meta tags, even to browsers, they require login, no rush, i've added some pretty fallback images as well


How do you extract meta tags specifically from the HTML?


as I've mentioned above, I've used cheerio (recommended by chatGPT: my new dear friend) and it's simple to use,now back to the 

question how do I extract? see the tags are inside the <meta> tags like : 

<meta data-rh=true property=og:url content=https://cheerio.js.org/ />

<meta data-rh=true property=og:locale content=en />

<meta data-rh=true property=og:title content="The industry standard for working with HTML in JavaScript | cheerio"/>

<meta data-rh=true property=og:description content="The fast, flexible & elegant library for parsing and manipulating HTML and XML."/>
so this is how my server will be receiving reponse form the specific url, so we need 2 things here: 


search for property selector from meta tags & 

extract the value from content selector, which is exactly what my function below does 


const getMeta = (...names: string[]) => {
            for (const name of names) {
const byProperty = $meta[property="${name}"]).attr("content");
if (byProperty) return byProperty;


const byName = $meta[name="${name}"]).attr("content");
if (byName) return byName;
            }
return "";
        };



some websites send the tags inside name selector instead of property selector, so we have also handled that gracefully!

What if a site blocks your requests despite headers?
Then it's either one of the below case: 
1. The URL is sensitive, the link cannot be saved, exits with proper HTTP status code 403 
2. The Link user entered it needs explicit sign-in, some websites expect you to be signed in
3. Our app's server maybe down, not your mistake
4. Rate Limiting - Making too many requests too quickly; the site's API/server throttles or blocks your IP temporarily
5. Cloudflare/DDoS Protection - Site uses Cloudflare or similar services that require JavaScript execution or browser challenges
6. Advanced Bot Detection - Despite proper headers, sophisticated systems (like DataDome, PerimeterX) detect scraping patterns
7. Geographic Restrictions - Site blocks requests from certain countries/regions where your server is hosted
8. IP Reputation Issues - Your server's IP might be flagged or blacklisted from previous abuse (not necessarily from you)
9. CAPTCHA Requirements - Site requires CAPTCHA completion which automated scraping can't handle
10. Robots.txt Restrictions - Site explicitly disallows scraping in their robots.txt file
11. Request Timeout - Site takes too long to respond (we have 15-second timeout to prevent hanging)
12. Missing Required Headers - Some sites need specific custom headers like Referer or Origin to be set properly 





~ Front-end Implementation: 


Front-end is coded in Nextjs, that's my go to library when I have to display something, Nextjs is really I love, it's the whole ecosystem, that Vercel vibe, 

UI components are inspired from shadcn & also components are from shadcn's library

I use dark / light theme as well for preferences, we devs just hate the light theme, but have to built cause of Muggles (non-devs)

there's Accordion as well which answers some questions user's might be having

Pagination added for professionalism


~ UI/UX Design & User Experience:


One thing I really focused on was making the application feel polished and professional, not just another CRUD app. Here's how I approached the UI/UX:


Component Library & Design System:
I used shadcn/ui as my component library - it's just chef's kiss! The components are accessible, customizable, and look modern out of the box. 
Used Radix UI primitives under the hood which means I get proper keyboard navigation, ARIA attributes, and focus management for free


Dark/Light Theme:
Implemented theme switching with next-themes. The theme persists across sessions and respects user's system preferences
Added a smooth theme toggle button with proper icons that make it obvious what it does
Both themes are carefully designed - dark theme for us devs who code at 2 AM, light theme for the "normal" people


Responsive Design Philosophy:
Mobile-first approach - tested on multiple devices to ensure it works everywhere
Grid layout automatically adjusts: 1 column on mobile, 2 on tablet, 3 on desktop
Items per page changes based on viewport: 3 on mobile, 4 on tablet, 6 on desktop
All buttons and interactive elements have proper touch targets for mobile users


User Feedback & Loading States:
Used sonner for toast notifications - they're non-intrusive and look beautiful
Every action gives feedback: success messages, error handling, loading spinners
Skeleton loaders while data is being fetched - users always know something is happening
Added tooltips everywhere to guide users on what each button does


Interactive Elements:
Pin functionality - users can pin important links to the top (with proper visual indicators)
Category system with emojis - makes organizing links fun and visual
Search functionality with CMD+K shortcut (because we're fancy like that)
Smooth animations and transitions using Tailwind CSS


Smart Pagination:
Responsive pagination that shows appropriate number of pages based on screen size
Scroll to top on page change - small detail but improves UX significantly
Current page highlighting and proper ellipsis for large page counts


View Modes:
Grid view for visual browsing (default)
List view for information-dense display
Toggle between them with beautiful icons
State persists during the session


Accessibility Considerations:
Proper semantic HTML structure
Keyboard navigation support (Tab, Enter, Escape, CMD+K)
Focus indicators on all interactive elements
ARIA labels and proper form labels
Screen reader friendly with proper heading hierarchy


Visual Hierarchy:
Clear separation between header, content, and footer sections
Card-based design for link previews with proper spacing
Consistent color scheme using CSS variables
Visual feedback for hover, active, and focus states


Empty States:
Designed meaningful empty states instead of blank screens
Different messages based on context (no links vs filtered category with no links)
Carousel for non-authenticated users showcasing features


Micro-interactions:
Hover effects on cards and buttons
Smooth color transitions
Loading states with spinners
Copy-to-clipboard with visual confirmation


Performance Considerations:
Lazy loading of images in link previews
Optimized re-renders with proper React hooks
Debounced search to avoid excessive filtering
Client-side caching where appropriate



~ Deployment details :


Whole codebase is deployed on Vercel, I have access to 2 customized domains but still I just love Vercel, it's easy to manage, debug and super easy

Authentication & Database is hosted on Supabase 

Version Control is Git obviously, repo is also private obviously twice (lol)

The fallback images I've used those are self-hosted in the app itself



~ User feedback :


Well the real user-feedback will be now, cause to all the people I showed my application, they gave me honest reviews and only some were technical, so 
that I've fixed however you're feedback is valuable, I've added a component in the app, you can drop you're thoughts there. appreciate that


~ DB schema (for v0.app)


I don't know that I should expose my schema, I know it doesn't hurt but I don't think it should be displayed, tell me you're views


~ Current state :


Currently my backend is live & hosted on supabase's free plan which is very generous, at this point my app should handle decent  
amount of users, as I am not expecting a huge crowd  I don't think I have the need to upgrade my plan, but if I need to I'll be more than happy


It can handle 1K users monthly that's good for a side-project / self-project, cause I myself use it heavily


~ Future Plans : 


I think make the website more beautiful, UI I think I've spent lesser time but still It gives a professional application vibes, I have a design in
mind I'll probably work after exams


maybe add some functionalities like I cannot think of anything, right now but I am willing to modify that's for sure 

One thing I have in mind, I saw a very pretty timeline component which I would very much like to use in my app, not sure where, if you're reading this  
do suggest me, thanks!

~ XSS Protection - A Critical Security Implementation:


This is probably the most important part of the application from a security standpoint. When you're scraping content from random websites
and displaying it to users, you're opening yourself up to XSS (Cross-Site Scripting) attacks. Malicious websites could inject harmful 
scripts through their metadata that would then execute in your users' browsers. Here's how I prevented that:


What is XSS and Why Should You Care?
XSS is when an attacker injects malicious JavaScript into your application. In a link preview app, this could happen if:
- A malicious website puts <script>alert('hacked')</script> in their og:title
- Someone creates a profile with javascript:void(0) in their og:url
- A site uses event handlers like onclick in their metadata
If you display this content without sanitization, the malicious code executes in your user's browser - game over!


My Multi-Layer XSS Protection Strategy:


1. Dedicated Sanitization Library (lib/sanitize.ts):
Created a comprehensive sanitization library with three main functions:
   - sanitizeText(): Strips HTML tags, removes scripts, removes event handlers, blocks javascript: protocol
   - sanitizeUrl(): Validates URLs and only allows http/https protocols
   - sanitizeImageUrl(): Special handling for images, allows data:image for base64 but blocks data:text
   - sanitizeMetadata(): Wraps all scraped data through appropriate sanitizers


2. Server-Side Sanitization (API Route):
Before storing ANY scraped metadata in the database, I sanitize it
This is crucial - never trust scraped content!
Example from my API route:
```
const sanitized = sanitizeMetadata({
    url, title, description, image, site_name, type, audio
});
// Only THEN insert into database
await supabase.from('links').insert(sanitized)
```


3. Client-Side Sanitization (Double Protection):
Even though data is sanitized on the server, I ALSO sanitize on the client before rendering
Implemented inline sanitization functions in link-preview-card.tsx
Defense in depth - if somehow unsanitized data makes it to the client, we catch it


4. Specific Attack Vectors Blocked:

HTML Tag Injection:
   - Regex: /<[^>]*>/g removes all HTML tags
   - Prevents: <script>malicious()</script> or <img onerror="hack()">

Script Tag Injection:
   - Regex: /<script\b[^<]*(?:(?!<\/script>)<[^<]*)*<\/script>/gi
   - Removes entire script blocks with their content

Event Handler Injection:
   - Regex: /on\w+\s*=\s*["'][^"']*["']/gi
   - Blocks: onclick, onerror, onload, and all other event handlers

JavaScript Protocol:
   - Check: javascript:, data:, vbscript:, file:, about:, blob:
   - Prevents: <a href="javascript:alert('xss')">click me</a>

URL Validation:
   - Use native URL() constructor to parse and validate
   - Only allow http: and https: protocols
   - Block all other protocols including file:, data:, etc.

HTML Entity Decoding:
   - Decode entities like &lt; &gt; &quot; to prevent double-encoding attacks
   - Then strip the actual < > characters

Content Length Limits:
   - Limit text to 1000 characters to prevent DOS attacks
   - Prevents someone from sending gigabytes of text in metadata


5. Special Cases Handled:

YouTube Links:
   - Use official YouTube oEmbed API (trusted source)
   - Still sanitize the response because never trust ANY external data

Fallback Images:
   - If image URL is invalid/dangerous, fall back to safe local images
   - Never break the UI due to security checks

Data URIs for Images:
   - Allow data:image/ for base64 encoded images (needed for some sites)
   - Validate format: /^data:image\/(png|jpeg|jpg|gif|webp|svg\+xml);base64,/i
   - Block data:text and data:application (dangerous!)


6. Logging and Debugging:
Added console.warn() for all blocked attempts
Helps with debugging and can be monitored in production
Format: console.warn('[XSS] Blocked dangerous URL scheme:', urlStr)


7. Testing Approach:
Tested with intentionally malicious URLs:
   - javascript:alert('xss')
   - <script>alert('xss')</script> in titles
   - Event handlers in descriptions
   - Invalid URL protocols
All blocked successfully!


Why This Matters:
One XSS vulnerability can compromise ALL your users
Attackers could steal session tokens, redirect to phishing sites, or inject crypto miners
Link preview apps are especially vulnerable because you're displaying untrusted content
Having this protection means users can safely save links from ANY website


The Bottom Line:
NEVER trust external data. ALWAYS sanitize before storage and rendering.
Implement defense in depth - sanitize multiple times at different layers.
Use whitelist approach (allow only safe things) rather than blacklist (block known bad things).


Real-World Impact:
Without this protection, a single malicious link could:
- Steal authentication tokens from other users viewing the link
- Redirect users to phishing sites
- Inject Bitcoin mining scripts
- Deface the entire application for all users
With this protection - all these attacks are blocked at multiple layers!


Instead of challenges faced, I would say what I avoided: 


Code without proper architecture

fearful to modify original codebase 

delete / add features

walked on a certain fixed path (I made some errors along the line)

in capital NO VIBE CODING

I Spent a lot of time off-coding exploring stuff, & learning concepts I used 

Going into internals of components, like error status codes, I know these are some basic stuff but they really do matter, I gotta know exactly why

Was using PUT all this time, whilst correct handles is PATCH (rookie mistake)

was consistent, avoided burnout

before adding a feature, wrote it down thoroughly

Did everything with 100% energy, not for just the sake of it 

Biggest Challenge suprisingly was to add authentication, I was just not able to do that mentally, the routes logic, RLS policies have tripped me a lot in past 


~ User Onboarding & Clear Call-to-Actions:


One challenge I faced was: how do you engage users who land on your application without being signed in? 
The answer: make the value proposition crystal clear and guide them naturally towards signing in.


First Impression Matters:
When a non-authenticated user lands on the app, here's what they see:
- A clean, professional link input interface (disabled with a helpful tooltip)
- A prominent "Sign in" button right in the header
- An avatar icon with UserRoundPlus that's clickable and inviting
- Tooltip that says "Click to Login" on hover


Progressive Disclosure:
Instead of a blank page or just a login wall, I show:
1. The main interface so users can see what they'll get
2. Disabled input with tooltip: "Sign in to submit links"
3. A carousel showcasing three key features/benefits
4. FAQ section at the bottom answering common questions


The Carousel Approach (Feature Showcase):
When users aren't logged in, instead of their saved links, they see an interactive carousel with:
- Feature explanations
- Use case examples
- Visual demonstrations
- Navigation arrows to explore all benefits
This lets users understand the value before committing to sign up


Strategic CTA Placement:
Primary CTA: Large "Sign in" button below the input (impossible to miss)
   - Uses secondary variant for better visibility
   - Has an icon (UserRoundPlus) making it obvious
   - Loading state when clicked (shows the app is responsive)

Secondary CTA: Avatar in the header
   - Always visible as user scrolls
   - Consistent placement across the app
   - Same redirectUser() function for consistency

Visual Cues:
- Input field is visibly disabled (grayed out)
- Cursor shows "not-allowed" when hovering disabled elements
- Tooltips explain WHY features are disabled


The Authentication Flow:
1. User clicks "Sign in" anywhere → redirectUser() function triggers
2. Shows loading state with spinner and "Loading..." text
3. Redirects to /login page
4. Offers Google and GitHub OAuth (no password fatigue!)
5. After auth, redirects back to main page
6. All features unlock automatically
7. Input gets focus automatically (ready to paste links)


Post-Authentication Experience:
Once logged in, the experience transforms:
- Input becomes active and gets auto-focus
- "Sign in" button changes to "Submit" button
- User email displays in the header
- Saved links appear (or empty state with guidance)
- All features unlock: categories, search, sort, filters


Empty State Design (For New Users):
Even after signing in, if users have no links, they see:
- Clear heading: "No link added yet"
- Actionable description: "Start by entering a link above"
- NOT a dead-end, but an invitation to act
- If filtering by category: specific message about that category


Tooltips Everywhere (Subtle Guidance):
- Help icon next to input explaining its purpose
- Hover on disabled input shows "Sign in to submit links"
- During auth check shows "Checking Authentication"
- Every icon button has a tooltip explaining its function
- Keyboard shortcuts shown in tooltips (CMD+K for search)


Zero Friction Authentication:
- Only OAuth (Google/GitHub) - no forms to fill
- No email verification loops
- Instant access after successful OAuth
- Session persists across visits
- Auto sign-out on session expiry with notification


Loading States & Feedback:
- Initial load shows "Checking Authentication"
- Button shows spinner during redirect: "Loading..."
- User always knows what's happening
- No confusing blank states or silent failures


Preventing Premature Actions:
- Input disabled until auth completes
- Submit button only appears for authenticated users
- Non-authenticated users can't accidentally trigger API calls
- Clear error messages if somehow they try


The Redirect Function:
```
const redirectUser = async () => {
    try {
        setIsRedirecting(true);
        router.push('/login');
    } catch (error) {
        toast.error(error);
        setIsRedirecting(false);
    }
}
```
Simple, clean, with proper loading and error states


Why This Approach Works:
- Users understand the value before signing up
- Multiple CTAs mean they can't miss the action
- Loading states prevent confusion
- OAuth makes signup effortless (2 clicks max)
- Post-auth experience is smooth and guided
- Empty states are helpful, not discouraging


Conversion Funnel:
1. Land on page (see the interface)
2. Try to interact (realize they need to sign in)
3. See carousel (understand the value)
4. Click "Sign in" (frictionless OAuth)
5. Return to app (everything works now)
6. Add first link (immediate value)
7. Become regular user


Accessibility in Onboarding:
- Keyboard navigation works for all CTAs
- Screen readers announce disabled states
- Focus indicators make the path clear
- No hidden or confusing navigation


Mobile Experience:
- All CTAs are thumb-friendly (proper touch targets)
- Responsive text and button sizes
- Carousel works with swipe gestures
- No tiny text or hard-to-tap elements


What I Learned:
- Don't hide your app behind a login wall
- Show value before asking for commitment
- Make sign-in obvious but not annoying
- Every state should guide the user forward
- Loading states are as important as the content
- OAuth is way better than email/password in 2025!


~ lessons learnt : 


Ideas don't matter, working on them matter

All features are not meant to be added in a day

Be accountable

Document you're experience

With each & every you grow 

A features a day keeps burnout out

Everyday is a different day, forget yesterday work today

Work in chunks of time

Take a walk 

Start fresh

Read a lot

Don't hesitate to tweak you're codebase

Another word is "get you're hands dirty" 

